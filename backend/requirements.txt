fastapi==0.104.1
uvicorn==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
scikit-learn==1.3.2
pandas==2.1.3
numpy==1.26.2
python-dotenv==1.0.0
pytest==7.4.3
httpx==0.25.2
pydantic-settings>=2.0.0
tensorflow-cpu==2.15.0; sys_platform != 'darwin' or platform_machine != 'arm64'
# For Mac Silicon (M1/M2/M3), often tensorflow-macos is needed, but for now we stick to standard.
# If strictly cpu is fine, standard tensorflow might work or use specific macos builds.
# We will use standard for now and user can adjust if on specific hardware.
